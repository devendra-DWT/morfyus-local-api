table:
  name: status_of_recommendations
  schema: public
is_enum: true
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
      filter: {}
  - role: customer
    permission:
      columns:
        - id
      filter: {}
  - role: freelancer
    permission:
      columns:
        - id
      filter: {}
